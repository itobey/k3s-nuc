apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: homer
  namespace: homer
spec:
  rules:
  - host: homer.nuc.local
    http:
      paths:
      - backend:
          service:
            name: homer
            port:
              number: 1337
        path: /
        pathType: ImplementationSpecific
