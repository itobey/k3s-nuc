apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: baikal-config-pvc
  namespace: baikal
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: baikal-config-pv
  resources:
    requests:
      storage: 1Gi
