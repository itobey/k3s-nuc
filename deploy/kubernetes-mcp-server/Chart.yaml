apiVersion: v2
name: kubernetes-mcp-server
description: Umbrella chart that pulls the upstream kubernetes-mcp-server OCI chart as a dependency
type: application
version: 0.1.0
appVersion: "0.1.0"

dependencies:
  - name: kubernetes-mcp-server
    version: "0.1.0"
    repository: "oci://ghcr.io/containers/charts"
