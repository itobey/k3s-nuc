apiVersion: v1
kind: PersistentVolume
metadata:
  name: gitea-mariadb-pv
  labels:
    type: local
spec:
  storageClassName: gitea-mariadb-pv
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/media/enc/docker-volumes/gitea-db"
