apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: misc
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: postgres-pv
  resources:
    requests:
      storage: 1Gi
